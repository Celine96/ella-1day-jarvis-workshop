---
description: 클로드 코드 설치·버전 검증
---

다음을 차례로 확인해 출력하세요:

1. 클로드 코드 버전 (`claude --version` 또는 환경변수 기반)
2. OS / 셸
3. `.claude/` 폴더 존재 여부
4. `.claude/agents/` `.claude/commands/` `.claude/routines/` 하위 폴더 존재 여부

✅ / ❌ 표시로 결과를 출력하고, 빠진 항목이 있으면 어떻게 보충할지 한 줄로 안내합니다.
