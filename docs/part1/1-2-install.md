# 1-2. MCP — 자비스가 외부 도구에 연결되는 방법

::: tip Step 2 / 13 · Part 1
이 챕터에서 — **자비스가 살 작업실(클로드 코드)을 준비하고, MCP 개념과 보안 원칙을 함께 잡아봅시다.**
:::

## 클로드 코드란

자비스는 클로드 코드라는 작업실 안에서 살고 일합니다. 작업실에서 자비스는 요한님의 페르소나 파일을 읽고, 슬래시 커맨드(`/morning-brief` 등)를 실행하고, MCP를 통해 외부 도구(Gmail·Calendar)에 다녀옵니다.

## 설치 확인

::: tip 슬래시 커맨드로 검증
클로드 코드에서 다음을 입력하세요.

```
/1-2-install-check
```

**기대 결과**: 클로드 코드 버전과 사용자 환경 정보가 출력됩니다.
:::

이미 설치되어 있다면 30초면 끝나요. 처음이면 설치 가이드는 [클로드 코드 공식 안내](https://docs.claude.com/claude-code)를 참고해주세요.

## MCP — 자비스가 외부 도구에 접근하는 통로

### 왜 MCP가 필요한가 — LLM의 두 가지 한계

자비스의 '머리'에 해당하는 LLM(대규모 언어 모델)은 강력하지만 두 가지 한계가 있어요.

1. **학습 시점에 멈춰 있음** — LLM은 학습한 시점까지의 지식만 알 수 있어요. 어제 받은 메일이나 오늘 일정은 그 자체로는 알 수 없습니다.
2. **외부 도구를 직접 쓸 수 없음** — 메일을 보내거나, 캘린더에 일정을 등록하거나, Notion에 글을 쓰는 행동은 할 수 없어요.

이 두 한계를 풀려면 LLM에게 **외부 도구·실시간 데이터에 접근할 통로**가 필요합니다. 그게 **MCP(Model Context Protocol)** — Anthropic이 2024년 11월 만든 개방 표준이에요.

### MCP = AI 세계의 USB-C

비유하자면, 컴퓨터·휴대폰의 **USB-C 포트**와 같아요. USB-C 하나면 충전기·외장 디스크·모니터까지 모두 연결되듯이, MCP 하나로 자비스가 Gmail·Calendar·Notion·Slack 등 모든 도구에 **표준화된 방식**으로 연결됩니다.

이전엔 도구마다 별도의 연결 방식이 필요했지만, MCP 덕분에 **자비스 ↔ 모든 도구**가 한 통로로 깔끔하게 묶여요.

### 자비스가 사용할 MCP

이번 워크숍에서 자비스는 **Gmail · Google Calendar(필수)** 와 **요한님이 평소 쓰시는 옵션 채널 1개**를 MCP로 연결해 사용합니다. 자비스의 5가지 핵심 기능(매일 브리핑·받은편지함 정리·회의 준비·회의록 처리·주간 회고)이 모두 이 MCP들을 통해 실제 데이터를 보고 정리하게 되는 구조예요.

![자비스 + MCP 구조](/jarvis-mcp.png)

각 MCP의 역할과 사용 기능:

| MCP | 자비스 안에서 하는 일 | 사용 기능 |
|---|---|---|
| **Gmail** (필수) | 미답 메일 조회 · 답장 초안 임시보관함 저장 · 자동 routine 자기발송 | ①②③④⑤ (전체) |
| **Google Calendar** (필수) | 오늘·다음 주 일정 조회 · 회의 참석자·시간 분석 | ①③⑤ |
| (옵션) **Tiro · Granola · Otter** | 회의록 가져오기 (3-2에서 한 가지 연결) | ④ |
| (옵션) **Notion · Slack · LinkedIn** | 학습자가 평소 쓰는 채널 1개 (3-3에서 연결) | ① 통합 |

::: tip 5가지 핵심 기능
- ① 매일 아침 자동 브리핑
- ② 받은편지함 자동 정리
- ③ 다음 회의 자동 준비
- ④ 회의록 → 액션아이템
- ⑤ 주간 회고·다음 주 우선순위
:::

본인 계정 인증은 **Session 2 시작 직후**에 진행합니다. 지금은 개념만 잡고 가면 됩니다.

### MCP가 자비스에 가져다주는 3가지

- **실시간 정보** — LLM 학습 데이터가 아닌, 요한님의 **오늘 실제 메일·일정**을 직접 보고 분류합니다.
- **할루시네이션 감소** — 외부 데이터를 직접 가져오니, 자비스가 "있지도 않은 메일"을 만들어내지 않아요.
- **표준화된 안전성** — 본인 인증·권한 범위가 명확합니다. 자비스가 약속된 작업만 하고, 그 이상은 하지 않아요.

## 자비스의 보안 원칙

본인 메일·일정에 자비스를 연결하는 일이라, 자비스를 설계하면서 보안 원칙을 시스템에 박아뒀습니다. 핵심 3가지:

### 발송은 절대 자비스가 하지 않습니다
- 답장 초안은 **Gmail 임시보관함**에 저장만 합니다
- "보내기" 버튼은 항상 요한님이 직접 클릭
- 자동 routine은 **본인 자기발송만** (외부 수신자 자동 발송 절대 금지)

### 권한은 언제든 1분 안에 회수
- Google 계정 보안 페이지에서 1분 안에 차단 가능
- 권한 회수 즉시 자비스는 본인 데이터에 접근 불가
- 자세한 회수 절차는 [부록 §B](/appendix#b-권한-회수-가이드)

### 결정은 항상 요한님
- 자비스의 사분면 분류·위임·삭제 추천은 모두 **추천**일 뿐
- 자비스는 결정 비용을 줄여드리지만, 결정 책임은 항상 본인
- 잘못된 추천을 따라가지 않을 자유가 보장됩니다

::: details 추가 보안 원칙 4가지 (펼쳐 보기)
- **단계별 권한 추가** — Session 2 읽기만 → Session 3 쓰기 추가 (자비스가 신뢰 가는 결과를 보여주기 전까진 쓰기 권한 X)
- **페르소나에 민감 정보 입력 금지** — 비밀번호·계좌·계약 금액·기밀 정보는 페르소나에 절대 입력 X
- **GitHub 공개 레포에 본인 데이터 안 올라감** — 워크북 콘텐츠·운영 코드만, OAuth 토큰은 클라우드 환경에서 격리 보관
- **분류가 틀리면 대화로 수정** — "이 발신자는 항상 Q4로요" → 페르소나 자동 진화

자세한 보안 체크리스트는 [부록 §D](/appendix#d-자비스-보안-체크리스트).
:::

::: warning 컴플라이언스 점검 (선택)
회사가 SOC 2·ISO 27001·GDPR 등 컴플라이언스 요구사항이 있다면 워크숍 전에 다음을 점검하세요.
- [Anthropic 데이터 처리 정책](https://www.anthropic.com/privacy)
- 회사 보안 정책상 외부 LLM에 메일 텍스트 전송 가능 여부
- 임시보관함 답장 초안에 들어갈 수 있는 정보 범위

회사 보안팀 검토가 필요하시면 워크숍 일정을 그 후로 잡으시는 것도 옵션입니다.
:::

> 💡 **자비스는 본인 데이터를 분류·정리해 추천만 합니다. 발송·결정은 항상 요한님이 직접.**

## 다음

이제 자비스가 살 작업실이 준비됐어요. 자비스에게 이름을 지어줄 시간 — [나만의 자비스 작명](/part1/1-3-naming).
